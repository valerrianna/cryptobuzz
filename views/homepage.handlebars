{{#each quotes as |quote|}}
{{> quote-info quote }}
{{/each}}

{{#each posts as |post|}}

{{> post-info post }}
</div>

{{else}}
<h6 class="pb-4 has-text-centered is-italic">There is no post yet</h6>
{{/each}}